clone:
  depth: full
pipelines:
  default:
    - step:
        name: Sync to GitHub
        clone:
          enabled: false
        script:
          - git push --mirror git@github.com:Siqsuruq/oodz.git